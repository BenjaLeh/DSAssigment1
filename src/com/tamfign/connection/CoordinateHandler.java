package com.tamfign.connection;

import java.net.Socket;

public class CoordinateHandler extends Handler {
	public CoordinateHandler(Socket socket) {
		super(socket);
	}

	protected void cmdAnalyse(String cmd) {
		// TODO
	}
}
